# Triage state — the loop's persistent memory

| finding | source | priority | status |
|---------|--------|----------|--------|
| Sweeps moved off Vercel -> trigger.dev (proj_pgxztmkkgkbkaptgxtvk, task documenso-sweeps, cron */15 SAST) | cutover 2026-08-17 | high | DONE — VERIFIED: all 7 sweeps return "ok", querying Aiven DB, $0.0001/run |
| Aiven free DB: single connection | user requirement | high | DONE — connection_limit=1 on trigger.dev + Vercel env vars |
| Telemetry disable | user requirement | high | DONE — DOCUMENSO_DISABLE_TELEMETRY=true on Vercel (no redeploy) + trigger.dev |
| Production sign-in page: styled, fonts 200, logo 200, zero console errors | playwright evaluator (loop-engineering/checks/prod-signin.check.js) | medium | DONE |
| /fonts/* 404 — CSS loaded fonts from /fonts/ but vercel.json only routed /assets/ to static fn | playwright evaluator first pass | high | FIXED — added /fonts/(.*) -> /api/static route, deployed, fonts return 200 |
| CSS/JS assets return 200 (verified); custom logo 200 image/png | curl checks | done | DONE |
| Sweeps endpoint: auth 200 {"ok":true}, bad token 401, 25 rate-limit rows cleaned | vercel logs | done | SUPERSEDED — endpoint removed from Vercel, sweeps run on trigger.dev |
| Function sizes split: /api 86.12MB, /api/cron 45.91MB, /api/static 63.51MB | vercel deploy | done | DONE — /api/cron now removed entirely, only 2 functions remain |
| Trusted Signatures integration (trusted-signatures transport) | user requirement | high | DONE — ported transport + helpers, verified end-to-end signing works |
| **[2026-08-21 MORNING] Entire feature set UNCOMMITTED on working tree** — white-label (emails+logo), cache-header fix (api/static.mjs), inline sweep handlers (5 handlers), @trigger.dev SDK 4.5.12 bump, lingui-macro-stub.ts, .triggerignore, vercel.json /static route, prisma migration 20260819060000. Meanwhile 10 unpushed local commits on main include now-SUPERSEDED ConvertAPI work ("feat: add ConvertAPI fallback", "feat: enable DOCX conversion flag when ConvertAPI secret") that the uncommitted tree deletes. History is inconsistent. | git status + git log origin/main..HEAD | high | DONE 2026-08-21 — committed as e630d69c ("feat: docx conversion via trigger.dev...") incl. green→black branding + centered h-32 email logo; pushed db0d6d29..e630d69c main->main (remote switched SSH→HTTPS via gh auth setup-git). render-check.tsx stray removed pre-commit. |
| **[2026-08-21 MORNING] CI has not validated any of these changes** — gh run list empty (nothing pushed to origin/main); workflows ci.yml/deploy.yml/e2e-tests.yml exist but never ran on this work. | gh run list (empty) | medium | CLOSED — repo has GitHub Actions effectively disabled (no runs fire even after push; workflows listed active but has_actions empty). Effective gates = Vercel build (typecheck passed every deploy) + trigger.dev container build (v20260820.11). |
| **[2026-08-21 MORNING] Stray temp file `render-check.tsx`** (root) untracked — leftover from email render verification, not a deliverable. | git status (??) | low | CLEANUP — remove |
| **[2026-08-21 MORNING] Live system GREEN** — Vercel aliased agreements.open-mic.co.za (deploys all Ready, cache headers immutable/HIT), trigger.dev v20260820.11 (3 tasks, sweeps inline-verified). No runtime regressions observed. | prior session verification | info | OK — no action |
| **[2026-08-29 EVENING] Jobs provider migrated Inngest → trigger.dev end-to-end** — added TriggerJobProvider (jobs/client/trigger.ts) + shared jobDefinitions registry (jobs/definitions.ts) + generic `documenso-job` task (apps/remix/trigger/jobs.ts); JobClient selects provider via NEXT_PRIVATE_JOBS_PROVIDER=trigger; removed Inngest provider, deps, env types, INNGEST_* secrets from Vercel. | full session | high | DONE — trigger v20260829.10 (4 tasks); documenso-job dispatch verified |
| **[2026-08-29 EVENING] Envelope document_2 SEALED** — via documenso-job on trigger.dev; status COMPLETED 17:03:50 SAST. Fixes en route: R2 storage creds (user-provided token), branding-logo ENOENT (defensive read + additionalFiles), presigner esbuild interop (externalized @aws-sdk/s3-request-presigner). | runs.retrieve + Aiven query | high | DONE — VERIFIED |
| **[2026-08-29 EVENING] Trigger env poisoning root cause** — syncVercelEnvVars (Vercel API) returns empty/[SENSITIVE] for Sensitive-flagged vars; sync wrote placeholders into Trigger (incl. 127.0.0.1:2500 SMTP default → seal email failures). Vercel write-only Sensitive vars are unreadable by ANY API (verified probe: vercel env pull redacts; single-GET decrypt=true refuses). | vercelSyncEnvVars.js source + probe test | high | ROOT CAUSE — documented; see inbox note |
| **[2026-08-29 EVENING] Notification emails GREEN** — user provided production Resend key; pushed NEXT_PRIVATE_SMTP_TRANSPORT=resend + RESEND_API_KEY + FROM_* into Trigger; live test send.password.reset.success.email → COMPLETED (delivered to kgothatso@open-mic.co.za). seal-document.handler email step made non-fatal (seal can never fail on email). | trigger run_06g4t2ove47u1v201eodrchp01 | high | DONE — VERIFIED |
| **[2026-08-29 EVENING] Vercel builds-mode dropped includeFiles → api/static served nothing** (css/js/fonts/favicon all 404). Fix: drop api/static.mjs entirely (dead code), static output layer serves assets: builds entry outputDirectory=apps/remix/build/client, routes filesystem-first → catch-all /api. | live 404 probes | high | IN PROGRESS — deploying static-fix |
| **[2026-08-29 LATE] Team-delete 500 root cause + fix** — `team.delete` reassigns orphaned data to a `deleted-account@<hostname>` service account; that User row was missing in Aiven DB (seed migration never created/renamed it for this host). Fixed by inserting `deleted-account@agreements.open-mic.co.za` into `User`. | psql on Aiven | high | DONE — verified row id 4; delete should now succeed |
| **[2026-08-29 LATE] i18n catalogs now bundle on Vercel** — bare side-effect `import '../../translations/x/web.mjs'` got tree-shaken by rollup (runtime MODULE_NOT_FOUND, fallback English). Fix: import as `import * as xCatalog` and hold in a *used* `staticCatalogs` map + `// @ts-ignore` (ambient `*.mjs` decl not in remix tsc graph). Verified 0 "Failed to load catalog" warnings post-deploy. | deploy + log probe | high | DONE — VERIFIED |
| **[2026-08-29 NIGHT] Certificate branding now shows the real crypto signer** — `renderBranding` in render-certificate.ts reads `NEXT_PRIVATE_SIGNING_TRANSPORT`; when `trusted-signatures` it renders "Cryptographic signature by: Trusted Signatures" and looks for `public/static/trusted-signatures-logo.png` (falls back to text if absent). Previously hardcoded Open Mic logo (misleading — implied Open Mic was the signer). | code + deploy | high | DONE — deployed to Vercel + Trigger |
| **[2026-08-29 NIGHT] Production Trusted Signatures key set + VERIFIED** — user provided prod API Key ID + secret. Disabled `syncVercelEnvVars` in trigger.config.ts (Vercel write-only poisoning would clobber Trigger env with `[SENSITIVE]`). Pushed `NEXT_PRIVATE_SIGNING_TRANSPORT=trusted-signatures` + both TS key vars to Trigger via API (`/envvars/prod/import`, body `{variables,override,isSecret}`). Direct API probe to api.trusted-signatures.com/v1/sign returned HTTP 200 with a CMS signature chained to "Trusted Signatures LLC" — **key authenticates & signs**. | Trigger API + probe | high | DONE — VERIFIED |
| **[2026-08-29 NIGHT] Trigger redeploy (cert + fonts)** — deployment `p40no9mi`. `additionalFiles` includes `./public/static` + `./public/fonts` (nicer signature handwriting); seal job now uses prod TS signer. | trigger.dev deploy | high | DONE |
| **[2026-08-29 NIGHT] Only prod TS key used — test key eradicated** — user confirmed only 1 prod key (ID 88e5bdbf-…); the other (test, `e5615fad-37…` / Vercel `eyJ2…`) must not be used. Replaced Vercel prod env `NEXT_PRIVATE_SIGNING_TS_API_KEY_ID` + `_SECRET` with prod values via Vercel API PATCH (type sensitive preserved). Trigger already had prod (override import). Web app does not call signPdf (signing only in Trigger worker) but Vercel now matches prod regardless. | Vercel API + Trigger API | high | DONE — no test key anywhere in deployment |
| **[2026-08-29 NIGHT] Trusted Signatures logo on cert** — downloaded `secure.trusted-signatures.com/images/logo.svg`, converted to PNG via sharp (2165 B) at `apps/remix/public/static/trusted-signatures-logo.png`. render-certificate.ts already renders it when transport=trusted-signatures. Bundled into Trigger worker via additionalFiles `./public/static` (deploy 891cj42i). | sharp + deploy | med | DONE |
| **[2026-08-29 NIGHT] Stale cached 404 fix (prevention)** — root cause: Vercel edge caches 4xx/5xx if response lacks no-store. Added middleware in `apps/remix/server/router.ts` setting `Cache-Control: no-store, must-revalidate` on any response status>=400, so transient errors can never be cached permanently. Redeployed Vercel (clears current stale cache). | code + deploy | high | DONE — VERIFIED (0 runtime errors post-deploy) |
| **[2026-08-29 NIGHT] Team delete 500 — second bug fixed** — after the deleted-account service user was created (first bug), `orphan-envelopes.ts:25,41` read `serviceAccount.ownedOrganisations[0].teams[0].id` but the service account owned no org/team → `Cannot read properties of undefined (reading 'teams')`. Created "Deleted Account" Organisation + Team (team id 13) owned by service account (id 4) via SQL. team.delete now resolves the reassignment target. | Aiven SQL | high | DONE — retry delete in UI |
| **[2026-08-30] Favicon white-labeled** — replaced default Documenso favicon with Open Mic asset from Desktop (`Open Mic/favicon.png`, 180x180). Generated favicon-16x16.png, favicon-32x32.png, favicon.ico in apps/remix/public; redeployed Vercel. Served size 1289B (was 579B Documenso). | sharp + deploy | low | DONE — VERIFIED |
| **[2026-08-30 NIGHT] Production outage root cause FIXED** — `vercel.json` had `builds` + `functions` (mutually exclusive) and committed `build.sh` never copied build → `api/build`, so git-based deploys crashed at runtime (`FUNCTION_INVOCATION_FAILED`, missing router.js). Restored `functions`-only config + `api/static.mjs` + full build.sh copy step. Production back to 200 on verified git deploy `a418004f`. | live 500s + Vercel API deploy inspect | high | DONE — VERIFIED all endpoints 200 |
| **[2026-08-30 NIGHT] Perf pass deployed** — 7 DB indexes (Aiven, migration 20260830000000_add_perf_indexes): Envelope.templateId, UserSecurityAuditLog(userId,createdAt), BackgroundJob(status,submittedAt)+(jobId,status), BackgroundJobTask(jobId)+(status), WebhookCall(webhookId,status,createdAt). getStats 60s TTL cache. Sign-page isRecipientAuthorized+getTeamSettings parallelized. Audit-log PDF capped 500. BackgroundJobTask.create idempotent (P2002 race). signin warm 1.35s→0.5-0.7s. | code + migration + live timing | high | DONE — VERIFIED |
| **[2026-08-30 NIGHT] BIMI live** — self-contained mic SVG (180x180, solid bg, no text) at agreements.open-mic.co.za/bimi/logo.svg; DNS `default._bimi.open-mic.co.za` updated to point at it (was dealroom wrapper SVG which Gmail rejects — external PNG). | DNS resolve + curl | med | DONE — record resolves, SVG 200 |
| **[2026-08-30 LATE] Security audit + fixes deployed** — audit (subagent): no CRITICALs. Fixed: (H1) removed committed Vercel preview-bypass token from AGENTS.md/LESSONS.md (rotate in dashboard); (M2) CSP now has default-src 'none' + connect-src/img-src/font-src/media-src; (M3) server-side upload size cap (was client-only); (M4) TSP prep path now enforces envelope.status=PENDING + assertRecipientNotExpired + not-signed/rejected (mirrors SES path). Left as documented tradeoff: frame-ancestors * on /sign,/d,/signin (customer embeds). Verified: preview + production all 200, CSP active. | audit + deploy f80d071a | high | DONE — VERIFIED |
| **[2026-08-30 LATE] Compromised preview-bypass token rotated** — old `TrreQfzu…` revoked + regenerated via `PATCH /v1/projects/{id}/protection-bypass` body `{"revoke":{"secret":"…","regenerate":true}}` (correct API shape). New token `rpbI2Zz5…` verified (old→302 blocked, new→200). Docs reference `$VERCEL_BYPASS_TOKEN` env var only. | Vercel API | high | DONE — VERIFIED |
